# Sentiment Analysis Task

`emotions` and `emotion_scores` retain their parallel-array shape. Validation
requires equal lengths (including when both are empty); each score belongs to
the emotion at the same index.

::: openaivec.task.nlp.sentiment_analysis
