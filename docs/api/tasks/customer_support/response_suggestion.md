# Response Suggestion

`response_style` accepts `professional` (default), `friendly`, `empathetic`,
`formal`, `apologetic`, or `solution_focused`. Each style appears in the
response schema's allowed `tone` values; unsupported styles raise `ValueError`.

::: openaivec.task.customer_support.response_suggestion
